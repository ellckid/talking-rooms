import styled from "@emotion/styled";

export const TitleContainer = styled.div({
  display: "flex",
  flexDirection: "row",
  justifyContent: "space-between",
  paddingBottom: "50px",
  alignItems: "center",
});
